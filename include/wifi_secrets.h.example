#pragma once

/**
 * @brief Example Wi-Fi secrets file copied to `wifi_secrets.h` for local development.
 *
 * Keep the real `wifi_secrets.h` out of version control and replace the placeholder
 * values with the credentials required for the target environment.
 */
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
